services:
  db:
    image: postgres:15
    container_name: shopping-db
    environment:
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
      POSTGRES_DB: shopping_cart
    ports:
      - "5432:5432"
